<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.eduservice.mapper.EduCourseMapper">
    <!-- 根据课程ID查询课程类别-->
    <select id="getSubject" resultType="com.atguigu.eduservice.entity.subject.SubjectVo">
        SELECT
            s.id,
            s.title
        FROM
            `edu_course` c,
            edu_subject s
        WHERE
            c.id = #{c}
            AND ( c.subject_id = s.id OR c.subject_parent_id = s.id )
    </select>
</mapper>
